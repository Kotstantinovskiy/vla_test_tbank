# Прогнозы (записаны 2026-08-18, до любого запуска обучения или оценки)

На основе априорных данных: наивный базовый вариант линии crislmfroes (`2026-08-16_self_smolvla_naive_learn_baseline`, аналогичный рецепт: только эксперт, 2000 шагов, батч 32, 20 эпизодов оценки) достиг средних значений по задачам ~0.86 для всех 10 целевых задач и 0.900/0.917/0.967 на задачах 0–2 при k=5/10/25, несмотря на несовпадение зеркалирования при обучении/оценке (mirror mismatch) и замену нормализатора в различных конвейерах конвертации.

Для данного запуска на предобучении на официальных данных (все конвенции согласованы, контроль на виденных задачах/seen-control 20/20, демо = официальные demo_0..demo_{k-1}):

1. **Среднее по 10 задачам**: k=5 >= 0.80, k=10 >= 0.85, k=25 >= 0.88, то есть на уровне старого базового варианта или немного выше; в среднем монотонно по k, с возможной немонотонностью по отдельным задачам при разрешении в 20 эпизодов (+-0.1).
2. **Задачи 0-2 (задание)**: сопоставимо с 0.90/0.92/0.97 или выше.
3. **Самые сложные задачи**: ожидаемые аутсайдеры — задача 3 (двухэтапная drawer+bowl) и задача 5 ("push", глагол не встречался при предобучении); прогноз: они все равно превысят 0.5 при k=25.
4. **Где может проявиться согласованность конвенций**: если разрыв в зеркалировании/домене снижал емкость адаптации, k=5 должен выиграть больше всего (требуется меньше демонстраций для преодоления несоответствия); среднее значение при k=5 явно выше 0.90 по 10 задачам подтвердило бы это.
5. Скачок k=0 -> k=5 (0.005 -> ~0.8) остается частично объяснимым заменой нормализатора; данный запуск не может разделить этот эффект (раскрыто в `protocol.yaml`), это могла бы сделать только абляция с фиксированной статистикой.

Факторы опровержения/сюрпризы: любая задача на уровне 0/20 для всех бюджетов (указывало бы на дефект конвейера для конкретной задачи, а не на ее сложность); среднее значение при k=25 ниже 0.86 старого базового варианта (указывало бы на то, что официальная конвертация повредила адаптации).
