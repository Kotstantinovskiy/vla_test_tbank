# Априорные ожидания — зафиксированы перед подготовкой и любым запуском оценки

Записано 23.08.2026, перед подготовкой, обучением или любой оценкой данного эксперимента.

Ориентиры (те же сиды/демонстрации/оценка, среднее по задачам 0–2, n=50, если не указано иное):

| метод | k=1 | k=2 | k=3 | k=5 | k=10 | k=25 |
|---|---:|---:|---:|---:|---:|---:|
| обучение только экспертных слоев (repro / naive repro) | 0.600 | 0.683 | 0.667 | 0.883 | 0.950 | 0.917 |
| полное тонкое обучение (2000 шагов) | 0.583 | 0.950 | 0.767 | — | — | — |
| полное тонкое обучение с аугментацией изображений (1000/1500/2000) | 0.583 | 0.900 | 0.783 | — | — | — |
| шум состояния α=0.10 на 1000 шагов (k=1) | 0.700 | — | — | — | — | — |

Лучшие известные результаты по бюджетам: k=1 0.700, k=2 0.950, k=3 0.783 (при n=50) / 0.817 (с аугментацией изображений при n=25), k=5/10/25 для наивного обучения только экспертных слоев 0.883/0.950/0.917.

Прогнозы для объединенной конфигурации (полное тонкое обучение + аугментация изображений + шум состояния α=0.10 + зависящий от объема данных бюджет шагов):

1. k=1 (при n=50): среднее значение ≥ 0.65 — выигрыш от добавления шума состояния сохраняется и при добавлении аугментации изображений (оба метода снижают переобучение через разные механизмы); взаимное уничтожение эффектов (среднее значение < 0.60) опровергло бы гипотезу об их аддитивности.
2. k=2 (при n=50): среднее значение в диапазоне [0.80, 0.95]. Факторы риска: влияние шума не тестировалось при k=2 и 1500 шагах; пиковое значение 0.950 при полном тонком обучении было обусловлено аномально высоким результатом на задаче с ящиком и является нестабильным.
3. k=3 (при n=50): среднее значение в диапазоне [0.70, 0.85], то есть на уровне или выше 0.767 для полного тонкого обучения.
4. k=5/10/25 (при n=50): объединенная конфигурация на основе полного тонкого обучения показывает результат на уровне или выше базовой линии обучения только экспертных слоев при k=5 (≥ 0.88) и находится в пределах ±0.05 от нее при k=10/25 (базовая линия уже близка к потолку; преимущество полного тонкого обучения уменьшается по мере роста объема данных). Падение ниже 0.85 при k=25 повторило бы задокументированный провал при k=25 на архивных кривых.
5. n=35 располагается между n=50 and n=25 при k≤2 и находится в пределах ±0.03 от лучшего из этих двух вариантов при k≥3; n=25 остается наихудшим вариантом при k=1 и примерно сравнивается с n=50 при k≥3 (аналогично поведению при аугментации изображений).
6. Монотонность: среднее значение по объединенной конфигурации (при n=50) не убывает с ростом k, за исключением максимум одного отклонения в пределах ≤ 0.05 (спад на задаче с ящиком при переходе k=2→k=3 может сохраниться).
7. Проверка детерминизма успешно проходится во всех трех вариантах.
8. Астрономическое время: при использовании схемы 2+2 слота на GPU весь конвейер (18 сессий обучения, 57 запусков оценки) завершается менее чем за 6 часов.

Действия при опровержении гипотезы: если объединенная конфигурация уступает лучшему изолированному результату более чем на 0.10 при любом бюджете данных, проанализируйте логи обучения (масштаб потерь при комбинированных аугментациях), аудит параметров и видеоролики оценки перед интерпретацией результатов; почечные различия в пределах ±2 успешных попыток на точку обусловлены шумом измерений для выборки из 20 эпизодов. Применяется оговорка об одном сиде обучения — требование задания использовать два сида остается невыполненным и является естественным следующим шагом (объединенная конфигурация + базовая линия на сиде 2).
